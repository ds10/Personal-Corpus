library(rjson)
library(foreach)
library(RMySQL)
library(tm)
library(RCurl)
library(twitteR)
require(topicmodels)
require(mallet)
require(rJava) # needed for stemming function 
library(Snowball) # also needed for stemming function 
require(pvclust)